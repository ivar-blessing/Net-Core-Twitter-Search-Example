{
  "Logging": {
    "LogLevel": {
      "Default": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AppSettings": {
    "Consumer": { // values from https://developer.twitter.com/en/apps/<APPID>
      "Key": "",
      "Secret": ""
    },
    "AuthorizeToken": "", // if left empty, make sure to enable 'GetAccessToken' in Search Service to set authentication token
    "HashTag": "Scotland"
  }
}
